package secondTask.entity;

public class ModelAnalyzer {
    /**
     * function to find smallest number between a and b
     * if are equal return a max integer
     *
     * @param temperature first number
     * @return return smallest number
     */
    public double analyzing(double temperature) {

        if (temperature >= 60) {
            return temperature;
        } else if (temperature >= 30) {
            return temperature;
        } else if (temperature >= 20) {
            return temperature;
        } else if (temperature >= 10) {
            return temperature;
        } else {
            return temperature;
        }
    }
}
